server:
  hostname: localhost
  port: 8080
  certs:
    cert: server.crt
    key: server.key
  repo:
    type: file
    path: ./files/
    # maven:
    #   baseURL: https://repo1.maven.org/maven2
    #   groupIdPrefix: com.example
    #   authMode: config
    #   username: user
    #   password: pass
    #   timeout: 30  # HTTP client timeout in seconds (default: 30)
  publish:
    maxSize: 204800
  auth:
    enabled: false
  packageCollections:
    enabled: true
    requirePackageJson: false